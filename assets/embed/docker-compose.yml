services:
  {{.ProjectName}}:
    build:
      target: dev
    platform: linux/amd64
    # command: "/$project_name"
    volumes:
      - .:/workspace
    #ports:
    #  - "8080:8080"
    #environment:
    #  - ENV_VAR=value
    networks:
      - devnet
  #db:
  #  image: postgres:latest
  #  environment:
  #    - POSTGRES_USER=user
  #    - POSTGRES_PASSWORD=password
  #    - POSTGRES_DB=mydb
  #  networks:
  #    - devnet

networks:
  devnet:
    driver: bridge
